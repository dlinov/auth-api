include "application.conf"

fp {
  db {
    url = "jdbc:h2:mem:myapp_db;MODE=MYSQL;DATABASE_TO_UPPER=FALSE;IGNORECASE=TRUE;INIT=RUNSCRIPT FROM 'classpath:init.sql'"
    user = ""
    password = ""
  }
}